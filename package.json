{
  "name": "loan-rover",
  "version": "1.0.0",
  "main": "./server/index.js",
  "license": "MIT",
  "scripts": {
    "build": "parcel build ./client/src/index.html --out-dir ./client/dist",
    "start": "node ./server/index.js",
    "postinstall": "cd ./client && yarn install && yarn prestart && cd ../server && yarn install"
  },
  "devDependencies": {
    "parcel": "^1.12.4",
    "prettier": "1.19.1"
  },
  "dependencies": {}
}
