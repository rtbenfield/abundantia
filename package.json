{
  "name": "loan-rover",
  "version": "1.0.0",
  "main": "./server/index.js",
  "license": "MIT",
  "scripts": {
    "build": "parcel build ./client/src/index.html --out-dir ./client/dist",
    "postinstall": "cd ./client && yarn install && cd ../server && yarn install"
  },
  "devDependencies": {
    "prettier": "1.18.2"
  }
}
